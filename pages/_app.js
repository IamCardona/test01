import 'antd/dist/antd.css'

import './styles.scss'
import './styles.css'

export default function({ Component, pageProps }) {
    return(
        <Component {...pageProps} />
    )
}

// Aviso de privacidad